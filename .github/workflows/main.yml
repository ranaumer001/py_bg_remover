name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    env:
      # AWS_REGION: us-east-1
      # ECR_REPO_NAME: flask-app
      # ECS_CLUSTER: my-cluster
      # ECS_SERVICE: my-service
      SECRET_KEY: klejnfoiq
      REMOVE_BG_API_KEY: bWcHUxjg5G5TVazF4Hg5FfKt

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker
        uses: docker/setup-buildx-action@v2

      # - name: Log in to Amazon ECR
      #   id: ecr-login
      #   uses: aws-actions/amazon-ecr-login@v1

      - name: Build and Tag Docker Image
        run: |
          docker build --build-arg klejnfoiq --build-arg bWcHUxjg5G5TVazF4Hg5FfKt -t Flask_App .

      # - name: Push Docker Image to ECR
      #   run: docker push ${{ steps.ecr-login.outputs.registry }}/$ECR_REPO_NAME:latest

      # - name: Deploy to Amazon ECS
      #   uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      #   with:
      #     cluster: ${{ env.ECS_CLUSTER }}
      #     service: ${{ env.ECS_SERVICE }}
      #     image: ${{ steps.ecr-login.outputs.registry }}/$ECR_REPO_NAME:latest
      #     aws-region: ${{ env.AWS_REGION }}
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
